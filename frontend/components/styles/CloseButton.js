import styled from 'styled-components';

const CloseButton = styled.button`
  color: black;
  font-size: 4rem;
  border: 0;
  position: absolute;
  z-index: 2;
  cursor: pointer;
  right: 20px;
  top: 0px;
`;

export default CloseButton;
